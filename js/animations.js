// Title animations
var typed = new Typed(".typing", {
    strings: ["Hello ASIP...", "Welcome to the story"],
    typeSpeed: 25,
    loop: true,
    backSpeed: 30,
    startDelay: 500,
    backDelay: 500,
});

dotNav("section", "easeInOutQuint");
