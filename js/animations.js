// Title animations
var typed = new Typed(".typing", {
    strings: ["Visualizing the LLM Industry"],
    typeSpeed: 40,
    backSpeed: 30,
    startDelay: 500,
    backDelay: 500,
});

dotNav("section", "easeInOutQuint");
